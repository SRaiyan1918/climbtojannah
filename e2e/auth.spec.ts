import { expect,test } from '@playwright/test';
test('email registration opens the private app',async({page})=>{const email=`user-${Date.now()}@example.test`;await page.goto('/register');await page.getByLabel('Name').fill('Test User');await page.getByLabel('Email').fill(email);await page.getByLabel('Password',{exact:true}).fill('StrongPass123!');await page.getByLabel('Confirm password').fill('StrongPass123!');await page.getByRole('button',{name:'Create account'}).click();await expect(page).toHaveURL(/verify-email/);await page.goto('/');await expect(page.getByRole('heading',{name:/Assalamu alaikum/})).toBeVisible();});
