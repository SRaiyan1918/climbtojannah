import { useState,type FormEvent } from 'react';import { Button } from '../../components/ui/Button';
export function SlipDialog({habitTitle,onRecord,onClose}:{habitTitle:string;onRecord:(note?:string)=>Promise<void>;onClose:()=>void}){const[note,setNote]=useState('');const[saving,setSaving]=useState(false);async function submit(e:FormEvent){e.preventDefault();setSaving(true);try{await onRecord(note);onClose()}finally{setSaving(false)}}return <div className="dialog-backdrop" role="presentation"><section className="dialog" role="dialog" aria-modal="true" aria-labelledby="slip-title"><h2 id="slip-title">Record a slip</h2><p className="muted">{habitTitle}</p><p>Recorded honestly. Start again from here.</p><form onSubmit={submit}><label>Private recovery note (optional)<textarea rows={4} value={note} onChange={e=>setNote(e.target.value)}/></label><div className="dialog-actions"><Button type="button" variant="ghost" onClick={onClose}>Cancel</Button><Button disabled={saving}>{saving?'Saving…':'Record & restart'}</Button></div></form></section></div>}
