import { useEffect,useState } from 'react';import { Button } from '../../components/ui/Button';import type { WeeklyReflection as Reflection } from '../data/schema';
export function WeeklyReflection({value,onSave}:{value:Reflection;onSave:(value:Reflection)=>Promise<void>}){const[draft,setDraft]=useState(value);useEffect(()=>setDraft(value),[value]);const fields:[keyof Pick<Reflection,'wins'|'struggle'|'nextFocus'|'gratitude'>,string][]=[['wins','What improved this week?'],['struggle','What was difficult?'],['nextFocus','What is your next focus?'],['gratitude','What are you grateful for?']];return <div className="weekly-form">{fields.map(([key,label])=><label key={key}>{label}<textarea rows={3} value={draft[key]} onChange={e=>setDraft({...draft,[key]:e.target.value})}/></label>)}<Button onClick={()=>void onSave({...draft,updatedAt:new Date().toISOString()})}>Save weekly reflection</Button></div>}
