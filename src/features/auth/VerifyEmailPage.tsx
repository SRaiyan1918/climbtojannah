import { Link } from 'react-router-dom';import { Card } from '../../components/ui/Card';import './authForms.css';export function VerifyEmailPage(){return <main className="auth-page"><Card className="auth-card"><div className="auth-brand"><span className="brand-mark" aria-hidden="true">۞</span><h1>Check your email</h1><p className="muted">We sent a verification link. Verify your email, then return to continue.</p><div className="auth-links" style={{justifyContent:'center'}}><Link to="/">Continue to app</Link></div></div></Card></main>}
