import { useState,type FormEvent } from 'react';import { Link } from 'react-router-dom';import { Button } from '../../components/ui/Button';import { Card } from '../../components/ui/Card';import { readableAuthError,sendPasswordReset } from './authService';import './authForms.css';export function ForgotPasswordPage(){const[email,setEmail]=useState('');const[message,setMessage]=useState('');const[error,setError]=useState('');async function submit(e:FormEvent){e.preventDefault();try{setError('');await sendPasswordReset(email);setMessage('If an account exists for this email, a reset link has been sent.')}catch(err){setError(readableAuthError(err))}}return <main className="auth-page"><Card className="auth-card"><h1>Reset password</h1><p className="muted">We’ll send a reset link to your email.</p><form className="auth-form" onSubmit={submit}><div className="field"><label htmlFor="email">Email</label><input id="email" type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></div>{error&&<p className="form-error" role="alert">{error}</p>}{message&&<p role="status">{message}</p>}<Button>Send reset link</Button><div className="auth-links"><Link to="/login">Back to sign in</Link></div></form></Card></main>}
